// ignore_for_file: prefer_const_constructors

import 'package:car_card/car_card.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('CarCard', () {
    test('can be instantiated', () {
      expect(CarCard(), isNotNull);
    });
  });
}
